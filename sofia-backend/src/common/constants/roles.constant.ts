export enum UserRole {
    EMPLOYEE = 'employee',
    SUPERVISOR = 'supervisor',
    ADMIN = 'admin',
    SUPERADMIN = 'superadmin',
}

export enum UserStatus {
    ACTIVE = 'active',
    BLOCKED = 'blocked',
    SUSPENDED = 'suspended',
    INACTIVE = 'inactive',
}
